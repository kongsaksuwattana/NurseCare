<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="NurseCare.SearchHNView">
    <VerticalStackLayout>
    <Grid ColumnDefinitions="*,Auto">
        <!--<zxing:CameraBarcodeReaderView
              x:Name="cameraBarcodeReaderView"
              BarcodesDetected="BarcodesDetected" />-->    
        <Entry Grid.Column="0" x:Name="SearchEntry" Placeholder="Search by Patient Name,HN" Margin="10" />        
        <ImageButton Grid.Column="1" Source="scancode.png" 
               VerticalOptions="Center" HorizontalOptions="End" 
               Margin="10" WidthRequest="30" HeightRequest="30"
                Clicked="ImageButton_Clicked"
               />
    </Grid>
        <Grid x:Name="CameraView"></Grid>
    </VerticalStackLayout>
</ContentView>